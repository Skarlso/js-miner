var expect = require('chai').expect
var dockerHelper = require('../utils/docker')

describe('Docker Helper', function () {
  describe('#setup', function () {
    it('sets up a given version', function () {
    })
  })
})
